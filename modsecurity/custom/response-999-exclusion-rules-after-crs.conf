# RESPONSE-999-EXCLUSION-RULES-AFTER-CRS.conf
# Keep empty by default.
# False-positive tuning protocol:
# 1) confirm offending rule id from audit log
# 2) scope exclusion by endpoint + target field only
# 3) re-run attack payload regression before merge
